[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Ignore the addon's __init__.py which uses relative imports
collect_ignore = ["__init__.py", "compute_nodes"]

# Add the tests directory to the path
pythonpath = .
